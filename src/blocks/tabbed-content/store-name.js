/**
 * The data store key used by the editor-only active-tab store.
 *
 * Lives in its own module so consumer blocks can import the constant
 * without pulling the store registration into their bundle.
 */
export const STORE_NAME = 'humanmade/tabbed-content';
